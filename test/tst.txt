PROMPT_BASE = """**ROLE:** Senior Medicinal Knowledge Extraction Expert (Diamond Standard)

**TASK:** Convert OCR text from page {PG} into a structured, knowledge-rich JSON. You are a logic-driven Scanner, not a summarizer.

---

### 1. GOLDEN RULE: "SCANNER" STRICT VERBATIM PROTOCOL

- **MANDATORY:** Transcribe every word and punctuation mark exactly as in the source. Do not alter, omit, or summarize any content.
- **NO SUMMARIZATION:** The JSON output must match the length and detail of the original text. Never use phrases like "as above," "similar," or any summary.
- **OCR NOISE REMOVAL:**  
    - Rejoin words split by line breaks (e.g., "hương \n phụ" → "hương phụ").
    - Remove non-text artifacts from chemical structures (e.g., stray "CH3", "OH", isolated dashes). Only capture descriptive text.
- **MISSING SECTIONS:** If a content section is absent, set its value to `null`. Never fill gaps with content from other sections.

---

### 2. SEMANTIC FIELD ASSIGNMENT (ERROR-PROOFING)

Assign text to the correct JSON fields based on actual meaning:

- **mo_ta_hinh_thai:** Verbatim description of roots, stems, leaves, flowers, fruits, including all measurements (cm, mm).
- **phan_bo_thu_hai_che_bien:** Exact details on habitat, harvest season, and all processing steps (e.g., Tứ chế, Thất chế). Do not omit or alter any step.
- **thanh_phan_hoa_hoc:** List all compound names, wrapping each active ingredient in `$`, e.g., `$leonurin$`, `$cyperen$`.
- **tac_dung_duoc_ly:** Full text of animal experiments (rabbit, dog, mouse) and clinical reports.
- **tinh_vi_quy_kinh:** Verbatim content for Taste, Nature, Channel Tropism.
- **chu_y_kieng_ky:**  
    - **AUTOMATIC INFERENCE:** If pharmacology mentions "Kích thích tử cung" or "Co bóp tử cung", add: "Thận trọng hoặc không dùng cho phụ nữ có thai (do tác dụng kích thích tử cung)".
    - **VERBATIM EXTRACTION:** Extract all sentences containing "Kỵ", "Kiêng", "Không dùng cho người...", "Hư hàn", "Thai phụ".
- **lieu_dung_chung:** Exact dosage instructions (e.g., "Ngày dùng 6 đến 12g dưới dạng thuốc sắc").

---

### 3. FORMULA EXTRACTION ("ANTI-LOSS" STRATEGY)

Each entry in `cac_bai_thuoc_raw` must follow this strict format:

`[Tên bệnh hoặc Chủ trị] | [Thành phần & Liều lượng] | [Cách dùng & Ghi chú] | [Nguồn]: {DIAMOND_SOURCE_ID}`

- **EXTRACTION RULES:**
    1. Identify all remedies starting with "Chữa...", "Trị...", "Dùng trong trường hợp...", OR proper names like "Cao...", "Đơn...", "Bài...". 
       - **Identity Rule:** For remedies without a proper name (e.g., "Chữa lỵ"), use the disease name for Block 1.
       - **Prepend Rule:** Always add the main herb name to ingredients (e.g., "40g Rau ngót leaves" instead of just "40g leaves").
    2. Do not miss any formulas embedded in running text (e.g., "Dùng chữa lỵ, liều 6g sắc uống" must be extracted).
       - **STRICT ATOMIC MAPPING:** You MUST separate content strictly into the correct blocks. Block 1 is ONLY for the disease/symptom name. Block 2 is ONLY for herbs and numbers (DO NOT put ingredients in Block 1). Block 3 is ONLY for verbs, preparation steps, and notes.
       - **No Internal Labels:** Do NOT echo template labels like "[Thành phần & Liều lượng]:" inside the blocks. Only output the actual extracted values.
       - **Noise Removal:** Strictly delete navigational text like "(Hm 3,4)", "(Hình 20)", "(1)", or "xem trang..." during extraction.
    3. If a formula lacks specific dosage or preparation details, use values from 'lieu_dung_chung'. Write it concisely in Block 2/3 (e.g., "6-12g (liều chung)"). DO NOT write long explanatory sentences like "Không có thông tin về liều lượng...". NEVER leave these blocks empty if general instructions are available.
    4. **Contextual Attachment & No Orphans:** Sentences that are notes, variations, or author's observations (e.g., "Có người dùng đơn này...", "Ngoài các loại kể trên...") MUST be appended to the **[Cách dùng & Ghi chú]** block of the preceding remedy, or ignored if irrelevant. DO NOT create a standalone entry for them.
    5. **FORCED STRUCTURE:** Every single entry in 'cac_bai_thuoc_raw' MUST strictly contain exactly 3 pipes (|) to separate the 4 blocks, regardless of whether the remedy is a simple prose sentence or a standardized formula. Do not merge them into a single text block.
- **PAGE CONTINUITY:** If a formula is cut off at the end of the page, extract up to the last word and DO NOT close with `| [Nguồn]`.

### 4. SECTION COORDINATION & SOURCE ANCHORING

- **Current Anchor:** {LAST_ANCHOR} | **Source ID:** {DIAMOND_SOURCE_ID}
- **ORPHANED FORMULAS:** If the page begins with a formula fragment (from the previous plant), append it to the object for {LAST_ANCHOR} before starting a new object.
- **ID FORMAT:** Always use `VI_THUOC_[TEN_VIET_HOA_KHONG_DAU]`.

---

**ULTIMATE DIRECTIVE:** Absolutely no summarization. Prioritize over-inclusion over omission—never miss any medical detail. Errors in dosage or contraindications can endanger users.
"""